@page "/post-cat-control-EDSEWHD"
@using DayanaWeb.Shared.EntityFramework.Entities.Blog;

<MudStack Justify="Justify.Center" Class="mt-3">
    <MudContainer MaxWidth="MaxWidth.ExtraLarge">
        <MudPaper Outlined="true">
            <MudExpansionPanels>
                <MudExpansionPanel Icon="@Icons.Material.Filled.Add" Text="Add New Category">
                    <AddPostCategory/>
                </MudExpansionPanel>
            </MudExpansionPanels>
        </MudPaper>
        <MudPaper Outlined="true" Class="mt-3">
            <MudTable Loading="true" ServerData="@(new Func<TableState, Task<TableData<PostCategory>>>(ServerReload))"
                      Hover="true" @ref="table" Bordered="true">
                <ToolBarContent>
                    <MudText Typo="Typo.h6">Post Category</MudText>
                    <MudSpacer/>
                    <MudTextField T="string" ValueChanged="@(s => OnSearch(s))" Placeholder="Search" Adornment="Adornment.Start"
                                  AdornmentIcon="@Icons.Material.Filled.Search" IconSize="Size.Medium" Class="mt-0">
                    </MudTextField>
                </ToolBarContent>
                <HeaderContent>
                    <MudTh>Name</MudTh>
                    <MudTh>Actions</MudTh>
                </HeaderContent>
                <RowTemplate>
                    <MudTd DataLabel="Category Name">@context.Name</MudTd>
                    <MudTd DataLabel="Actions">
                        <MudIconButton Icon="@Icons.Material.Filled.Delete" Color="Color.Error" OnClick="@(() => OnDelete(context.Id))"></MudIconButton>
                        <MudIconButton Icon="@Icons.Material.Filled.Edit" Color="Color.Secondary" OnClick="@(() => Edit(context.Id))"></MudIconButton>
                    </MudTd>
                </RowTemplate>
                <PagerContent>
                    <MudTablePager/>
                </PagerContent>
            </MudTable>
        </MudPaper>
    </MudContainer>
</MudStack>